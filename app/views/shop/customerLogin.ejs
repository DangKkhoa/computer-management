<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/css/output.css">
    <script src="https://kit.fontawesome.com/ad1797946c.js" crossorigin="anonymous"></script>
    <style>
        th, td {
            text-align: left;
            padding: 12px 4px;
            border-bottom: 1px solid rgb(148, 163, 184);
            
        }
        
        th:last-child, td:last-child {
            text-align: right;
            
        }

        tfoot > tr:last-child > td {
            border-bottom: none;
        }
    </style>

</head>
<body class="h-screen bg-slate-200">
    <div class="container-xl h-full">
        <%- include('shopNavbar') %>
        <main class="px-5 w-full">
            <div class="relative bg-white w-1/2 m-auto  gap-x-4 p-10 rounded ">
                <p class="text-center text-4xl mb-2 font-bold">Login</p>
                <form action="" class=" w-1/2 m-auto" >
                    <div class="mb-4 w-full">
                        <label for="phone" class="block font-bold">Phone number</label>
                        <input type="text" id="phone" class="w-full border-2 border-slate-300 p-2 rounded-lg" placeholder="012xxx4567" >
                    </div>
                    <div class="mb-4">
                        <label for="password" class="block font-bold">Password</label>
                        <input type="password" id="password" class="w-full border-2 border-slate-300 p-2 rounded-lg" placeholder="************">
                        
                    </div>

                    <div class="mb-4">
                        <input class="" type="checkbox" onclick="toggleShowPassword()"> Show password
                    </div>
                      <div id="messageBox" class="hidden">
                        <div class="bg-red-300 text-center p-2 mb-2 rounded-md">
                          <p class="text-red-900" id="message"></p>
                        </div>
                      </div>

                    <div class="flex flex-col items-center justify-between flex-wrap">
                      <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-3 px-4 rounded focus:outline-none focus:shadow-outline w-full" type="submit">
                        Login
                      </button>
                      <p class="my-6">Or</p>
                      <a class="inline-block align-baseline font-bold text-base text-blue-500 hover:text-blue-800" href="http://localhost:3000/customer/register">
                        <span>Create an account</span>
                      </a>
                    </div>
                </form>
                
            </div>
            
        </main>
    </div>
    
    <script>
        var passwordInput = document.getElementById('password');
        function toggleShowPassword() {
            if(passwordInput.type === 'password') {
              passwordInput.type = 'text';
            }
            else {
              passwordInput.type = 'password';
            }
        }
    </script>
</body>
</html>